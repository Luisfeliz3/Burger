<h1>Burgers !</h1>

<h2>Burgers that are not Eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless ate}}
      {{> burgers/burger-block ate=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are Ate!</h2>

<ul>
  {{#each burgers}}
    {{#if ate}}
      {{> burgers/burger-block ate=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="ate"Ate?</label><br>
    <input type="radio" name="ate" value="1" checked> Eating!<br>
    <input type="radio" name="ate" value="0"> Ate
  </div>

  <button type="submit">Add Burger</button>
</form>
