<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}"data-devour="{{devour}}">
		{{#if devour}}EAT IT!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Pass!</button>
</li>
